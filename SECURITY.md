# 安全性說明

## 作品定位

本專案是作品集 MVP，尚未經過獨立資安稽核或滲透測試。請勿使用它保存金融、政府帳號、主要 Email 等高價值憑證。

## 資料保存方式

- 密碼庫內容在瀏覽器中使用 Web Crypto API 加密。
- 加密資料只存於目前網址、瀏覽器及裝置的 `localStorage`。
- 專案沒有帳號系統、雲端資料庫、Email 找回或自動跨裝置同步。
- GitHub Repository 只保存程式碼，不會自動取得瀏覽器中的密碼庫。

## 跨裝置使用

- 加密 JSON 是手動備份與搬移檔案，不是即時同步服務。
- 匯入會覆蓋目前裝置的密碼庫，不會合併不同裝置的修改。
- 私人雲端可以存放加密備份，但雲端帳號應啟用兩步驟驗證。
- 建議指定一台主要裝置，修改後重新匯出並保留日期版本，避免新舊備份混淆。

## 備份注意事項

- 不要把 `cyber-talisman-backup-*.json` 上傳到公開 Repository。
- 不要上傳填寫完成的紙本復原卡、總管法印或含真實帳密的截圖。
- 恢復密碼庫需要同時具備加密 JSON 備份與正確的總管法印。
- PWA 可離線開啟不等於密碼庫已經備份。

## 回報安全問題

請勿在公開 Issue 中張貼真實密碼、總管法印、備份檔、Token 或個人資料。公開專案前，作者應先建立不公開的安全問題聯絡方式。
